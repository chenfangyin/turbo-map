# =============================================================================
# Turbo Map 环境变量配置
# =============================================================================

# =============================================================================
# 必需的环境变量 (Required)
# =============================================================================

# NPM 发布配置
# 注意：请设置你的真实 NPM_TOKEN，不要提交到版本控制
# 示例: NPM_TOKEN=npm_xxxxxxxxxxxxxxxx
NPM_TOKEN=
NPM_REGISTRY=https://registry.npmjs.org/

# =============================================================================
# 可选的环境变量 (Optional)
# =============================================================================

# GitHub 配置 (在 GitHub Actions 中自动提供，本地开发可选)
# GITHUB_TOKEN 由 GitHub Actions 自动提供，无需手动设置
# GITHUB_TOKEN=ghs_xxxxxxxxxxxxxxxx
GITHUB_REPOSITORY=chenfangyin/turbo-map
GITHUB_ACTOR=chenfangyin

# 发布配置
RELEASE_CHANNEL=stable
AUTO_PUBLISH=true
DRY_RUN=false

# 测试配置
TEST_COVERAGE_THRESHOLD=80
TEST_TIMEOUT=30000
PARALLEL_TESTS=true

# 构建配置
BUILD_TARGETS=esm,cjs,umd
MINIFY=true
GENERATE_SOURCEMAP=true

# 安全配置
SECURITY_AUDIT_LEVEL=moderate
DEPENDENCY_CHECK=true
LICENSE_CHECK=true

# 调试配置
DEBUG=false
VERBOSE_LOGGING=false

# =============================================================================
# 高级配置 (Advanced - 可选)
# =============================================================================

# NPM 作用域配置 (可选)
# 无作用域: 留空
# 个人作用域: @chenfangyin
# 组织作用域: @your-organization
NPM_SCOPE=

# 文档部署配置 (如果使用 GitHub Pages)
DOCS_BRANCH=gh-pages
# 自定义域名 (可选)
# 如果没有自定义域名，将使用默认的 GitHub Pages 域名
# 默认域名格式: https://[username].github.io/[repository-name]
# 例如: https://chenfangyin.github.io/turbo-map
DOCS_DOMAIN=

# 通知配置 (可选)
SLACK_WEBHOOK_URL=
DISCORD_WEBHOOK_URL=
EMAIL_NOTIFICATIONS=false

# =============================================================================
# 使用说明
# =============================================================================
# 1. 复制此文件为 .env
# 2. 设置必需的环境变量 (NPM_TOKEN)
# 3. 根据需要调整可选配置
# 4. 运行 npm run config:validate 验证配置
# 5. 运行 npm run config:generate-env 生成 .env 文件
#
# 安全注意事项:
# - 不要将真实的 NPM_TOKEN 提交到版本控制
# - 可以在运行时通过环境变量设置: NPM_TOKEN=your_token npm run release
# - 在 CI/CD 中通过 secrets 设置 NPM_TOKEN
# - GITHUB_TOKEN 在 GitHub Actions 中自动提供，本地开发时可选
# - 如果只在本地开发，可以忽略 GitHub 相关配置
# - 发布到 npm 只需要 NPM_TOKEN
# - NPM_SCOPE 用于发布到特定作用域，留空则发布到公共仓库 
